# ck-pi-extension

GitHub 仓库：`1837620622/ck-pi-extension`
本机路径：`/Users/chuankangkk/Downloads/ck-pi-extension`

这是 Pi 插件合集。以后新插件都放 `packages/`，根目录 `package.json` 的 `pi.extensions` 按需追加。

当前发布：`packages/pi-rail` 状态栏。

安装：

```bash
pi install npm:ck-pi-extension
pi install git:github.com/1837620622/ck-pi-extension
```

测试：`npm test`

不要把 `~/.pi/agent` 里的密钥写进仓库。
