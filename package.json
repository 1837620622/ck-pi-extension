{
  "name": "ck-pi-extension-workspace",
  "private": true,
  "type": "module",
  "description": "Monorepo for CK Pi extensions. NOT published: install ck-pi-rail / ck-pi-redkit individually.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "pack:check": "npm pack --workspaces --dry-run"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.0",
    "@earendil-works/pi-tui": "^0.85.0",
    "esbuild": "0.28.1"
  }
}
